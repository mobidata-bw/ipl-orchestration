{
  admin off
}

http://:80 {
  # IPL assets
  handle_path /datasets/* {
    root * /var/www/datasets
  }

  encode zstd gzip
  file_server {
    precompressed gzip zstd
  }
}

http://:81 {
  root * /var/goaccess/report
  encode zstd gzip
  file_server
}
